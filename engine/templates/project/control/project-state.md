---
project_id: "{{PROJECT_ID}}"
stage: control
niche: "{{NICHE}}"
location: "{{LOCATION}}"
---
# Project State — {{PROJECT_NAME}} (continuity dashboard)

The continuity source of truth for long builds: page 30 must not forget decisions
made while producing page 3. This file is inlined into EVERY job packet, so
whatever is written here is inherited by all subsequent work. Machine state
(stage statuses, canonical artifacts) lives in project.yml; human decisions and
notes live here. Update it in every approval PR and every page-closing PR.

## Active decisions


## Internal-linking notes
(planned link relationships: parent → child, service → method, problem → solution,
symptom → diagnosis, service → cost, service → location, supporting → commercial)

## Cannibalization notes
(intent ownership: primary entity → primary intent → owning URL → supporting URLs)

## Open questions
(unresolved items — mark as [REQUIRES CONFIRMATION]; hard QC blocks approval of
any artifact still carrying that marker)

## Next step

