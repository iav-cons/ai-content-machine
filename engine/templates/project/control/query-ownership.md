---
project_id: "{{PROJECT_ID}}"
stage: control
niche: "{{NICHE}}"
location: "{{LOCATION}}"
---
# Query Ownership Ledger — {{PROJECT_NAME}}

One question, one owner — across the whole site, including FAQs. Every
include_in_section / include_in_faq / mention_briefly verdict in a query-triage
artifact adds a row here (via the same PR). Hard QC fails duplicate queries
with different owners. Format:

| query | owner page id | verdict | added |
|---|---|---|---|
